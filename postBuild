#!/bin/bash
set -ex

# Install npm packages
npm install

# Make scripts executable
chmod +x run_single.sh run_multi.sh

# Create output directory
mkdir -p tokens

# Verify chromium
which chromium-browser || which chromium || echo "Warning: chromium not found"

# Test that chromium can at least show version
chromium-browser --version || chromium --version || true
